# Evolgeist Akashic Proxy

## Descrição
Proxy seguro para chamar IA (OpenAI / Gemini). O app chama este endpoint, sem expor chaves.

## Variáveis de ambiente
Copie `.env.example` → `.env` e preencha.

## Run local
```bash
cd server
npm install
npm run start
# ou
npm run start:dev